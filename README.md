# Ghostty configuration

## How to clone correctly the repository

> Just remember to save the cloned repository as `ghostty` to avoid issues.

```bash
cd ~/.config
git clone https://github.com/filippofontana/ghostty-config.git ghostty
```
